BOT_TOKEN="Discord bot token" # (you can get it from https://discord.com/developers/applications). Don't share it with random people btw
BOT_COLOR="#97aee8" # Color that bot will use for embeds

# Type of DB
DATABASE_NAME="sqlite3 | postgres" # Database

# Only for postgresql
HOST="url.with.your.db.dev"
USERNAME="postgresprobably"
PASSWORD="databaseuserpassword"
DATABASE="databasename"
PORT="5432" # Or the one you use

# Only for sqlite
FILE_PATH="tapris.db"
